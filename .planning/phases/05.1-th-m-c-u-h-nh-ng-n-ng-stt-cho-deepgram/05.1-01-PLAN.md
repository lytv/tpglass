---
phase: 05.1-th-m-c-u-h-nh-ng-n-ng-stt-cho-deepgram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/settingsService.js
  - src/bridge/featureBridge.js
  - src/preload.js
  - src/ui/settings/SettingsView.js
  - src/features/listen/stt/sttService.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can select STT language in Settings view via dropdown"
    - "Selected language persists across app restarts via electron-store"
    - "Deepgram uses the selected language for speech-to-text transcription"
    - "Default language is 'en-US' on first use"
  artifacts:
    - path: "src/features/settings/settingsService.js"
      provides: "sttLanguage defaults and getSttLanguage/setSttLanguage handlers"
      contains: "sttLanguage.*en-US"
    - path: "src/bridge/featureBridge.js"
      provides: "IPC handlers for sttLanguage get/set"
      contains: "settings:getSttLanguage|settings:setSttLanguage"
    - path: "src/preload.js"
      provides: "Preload API exposure for STT language"
      contains: "stt.*getLanguage|stt.*setLanguage"
    - path: "src/ui/settings/SettingsView.js"
      provides: "UI for STT language selector"
      contains: "sttLanguage|STT Language"
    - path: "src/features/listen/stt/sttService.js"
      provides: "Read stored STT language when initializing sessions"
      contains: "getSttLanguage|sttLanguage"
  key_links:
    - from: "SettingsView.js"
      to: "settingsService.js"
      via: "window.api.settings.setSttLanguage"
      pattern: "setSttLanguage"
    - from: "sttService.js"
      to: "settingsService.js"
      via: "getSttLanguage IPC call"
      pattern: "getSttLanguage"
---

<objective>
Add user-configurable Deepgram STT language settings. Users can select their preferred STT language from a dropdown in Settings, which persists across app sessions. Deepgram will use the selected language for speech-to-text transcription.
</objective>

<context>
@src/features/settings/settingsService.js (lines 11-19: defaults, lines 427-456: translation handlers)
@src/bridge/featureBridge.js (lines 33-34: translation IPC handlers)
@src/preload.js (lines 322-330: translation API)
@src/ui/settings/SettingsView.js (lines 683-720: language selector UI, lines 804-814: handleSelectLanguage)
@src/features/listen/stt/sttService.js (lines 152-153: initializeSttSessions with language)

Key patterns from translation settings:
- electron-store defaults for persistent storage
- getTranslationSettings/setTranslationSettings handlers with windowNotificationManager
- IPC registration in featureBridge.js
- Preload API exposure with listeners for settings changes
- SettingsView dropdown UI with filtered language search

Locked decisions from CONTEXT.md:
- Default to 'en-US' on first use
- Store in electron-store under 'sttLanguage' key
- Follow existing translationEnabled/translationLanguage pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sttLanguage to electron-store and create handlers in settingsService.js</name>
  <files>src/features/settings/settingsService.js</files>
  <action>
1. Add `sttLanguage: 'en-US'` to electron-store defaults (around line 17)
2. Add getSttLanguage() function following getTranslationSettings pattern (around line 456):
   - Try to get settings.sttLanguage
   - Return { language: settings.sttLanguage || 'en-US' }
   - On error, return { language: 'en-US' }
3. Add setSttLanguage({ language }) function following setTranslationSettings pattern:
   - Get settings, update settings.sttLanguage = language
   - Save settings
   - Call windowNotificationManager.notifyRelevantWindows('stt-language-updated', { language })
   - Return { success: true, language }
4. Export both functions in module.exports (around line 495)
  </action>
  <verify>
    <automated>grep -n "getSttLanguage\|setSttLanguage" src/features/settings/settingsService.js</automated>
  </verify>
  <done>getSttLanguage and setSttLanguage functions exist in settingsService.js, exported, sttLanguage added to defaults</done>
</task>

<task type="auto">
  <name>Task 2: Register IPC handlers in featureBridge.js</name>
  <files>src/bridge/featureBridge.js</files>
  <action>
1. Add IPC handler for 'settings:getSttLanguage' (around line 34):
   `ipcMain.handle('settings:getSttLanguage', async () => await settingsService.getSttLanguage());`
2. Add IPC handler for 'settings:setSttLanguage' (around line 35):
   `ipcMain.handle('settings:setSttLanguage', async (event, { language }) => await settingsService.setSttLanguage({ language }));`
  </action>
  <verify>
    <automated>grep -n "settings:getSttLanguage\|settings:setSttLanguage" src/bridge/featureBridge.js</automated>
  </verify>
  <done>IPC handlers registered in featureBridge.js</done>
</task>

<task type="auto">
  <name>Task 3: Expose STT language API in preload.js</name>
  <files>src/preload.js</files>
  <action>
Add STT API section after translation section (around line 330):
```javascript
// STT Language API
stt: {
    getLanguage: () => ipcRenderer.invoke('settings:getSttLanguage'),
    setLanguage: (language) => ipcRenderer.invoke('settings:setSttLanguage', { language }),
    onSttLanguageUpdated: (callback) => ipcRenderer.on('stt-language-updated', callback),
    removeOnSttLanguageUpdated: (callback) => ipcRenderer.removeListener('stt-language-updated', callback)
}
```
  </action>
  <verify>
    <automated>grep -n "stt:" src/preload.js | head -5</automated>
  </verify>
  <done>STT API exposed in preload.js with getLanguage, setLanguage, and event listeners</done>
</task>

<task type="auto">
  <name>Task 4: Add STT language selector UI in SettingsView.js</name>
  <files>src/ui/settings/SettingsView.js</files>
  <action>
1. Add STT language list (can use existing LANGUAGES array but need BCP-47 codes for Deepgram):
   Add static STT_LANGUAGES array with Deepgram-supported languages in BCP-47 format (e.g., 'en-US', 'es', 'fr', 'de', 'it', 'pt', 'ru', 'zh', 'ja', 'ko', 'ar', 'hi', 'nl', 'pl', 'tr', 'vi', 'sv', 'da', 'no', 'fi')
2. Add state properties in constructor (around line 688):
   - this.sttLanguage = 'en-US';
   - this.isSttLanguageDropdownOpen = false;
   - this.sttLanguageSearchQuery = '';
3. Add loadSttLanguage() method similar to loadTranslationSettings():
   - Call window.api.stt.getLanguage()
   - Set this.sttLanguage
4. Add handleSttLanguageSelect(langCode) method similar to handleSelectLanguage:
   - Call window.api.stt.setLanguage(langCode)
   - Update this.sttLanguage on success
5. Add render for STT language selector in Settings UI (new section after translation):
   - Label: "Speech Recognition Language"
   - Dropdown showing current selection
   - Searchable list (reuse existing pattern from translation)
6. Call loadSttLanguage() in loadInitialData() (around line 688)
  </action>
  <verify>
    <automated>grep -n "sttLanguage\|STT Language" src/ui/settings/SettingsView.js | head -10</automated>
  </verify>
  <done>STT language selector UI renders in Settings view, loads and saves settings</done>
</task>

<task type="auto">
  <name>Task 5: Modify sttService.js to read stored STT language</name>
  <files>src/features/listen/stt/sttService.js</files>
  <action>
1. Import settingsService at top of file (if not already imported):
   `const settingsService = require('../../settings/settingsService');`
2. Modify initializeSttSessions(language = 'en') function (line 152):
   - Before using the language parameter, try to get stored STT language
   - Use try/catch to handle case when settings not available (like translation pattern)
   ```javascript
   async initializeSttSessions(language = 'en') {
       let storedLanguage = 'en-US';
       try {
           const sttSettings = await settingsService.getSttLanguage();
           if (sttSettings?.language) storedLanguage = sttSettings.language;
       } catch (e) {
           console.log('[SttService] Using default language - settings not available');
       }
       const effectiveLanguage = process.env.OPENAI_TRANSCRIBE_LANG || language || storedLanguage || 'en-US';
   ```
3. The rest of the function remains unchanged - effectiveLanguage is used throughout
  </action>
  <verify>
    <automated>grep -n "getSttLanguage\|storedLanguage" src/features/listen/stt/sttService.js</automated>
  </verify>
  <done>sttService reads stored STT language when initializing sessions, falls back gracefully on error</done>
</task>

</tasks>

<verification>
- [ ] electron-store defaults includes sttLanguage: 'en-US'
- [ ] getSttLanguage and setSttLanguage handlers exist and work
- [ ] IPC handlers registered in featureBridge
- [ ] Preload exposes window.api.stt.getLanguage/setLanguage
- [ ] SettingsView renders STT language dropdown
- [ ] Selecting language saves to electron-store and persists
- [ ] Deepgram uses selected language for transcription
- [ ] Default is 'en-US' when no preference stored
</verification>

<success_criteria>
User can select STT language in Settings, choice persists across restarts, Deepgram uses selected language for transcription
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-th-m-c-u-h-nh-ng-n-ng-stt-cho-deepgram/05.1-01-SUMMARY.md`
</output>
