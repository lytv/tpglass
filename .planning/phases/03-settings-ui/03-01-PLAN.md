---
phase: 03-settings-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/settingsService.js
  - src/preload.js
  - src/ui/settings/SettingsView.js
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "User can toggle translation ON/OFF in Settings view"
    - "User can select target language from dropdown list"
    - "Translation settings persist across app restarts"
  artifacts:
    - path: "src/features/settings/settingsService.js"
      provides: "IPC handlers for translation settings (get/set)"
      exports: ["settings:getTranslationSettings", "settings:setTranslationSettings"]
    - path: "src/preload.js"
      provides: "Context bridge for translation settings"
      exports: ["settingsView.getTranslationSettings", "settingsView.setTranslationSettings"]
    - path: "src/ui/settings/SettingsView.js"
      provides: "Translation toggle and language selector UI"
      contains: ["translationEnabled", "selectedLanguage", "toggle-switch", "language-dropdown"]
  key_links:
    - from: "SettingsView.js"
      to: "preload.js settingsView"
      via: "window.settingsView.getTranslationSettings()"
      pattern: "settingsView\\.getTranslationSettings"
    - from: "SettingsView.js"
      to: "settingsService.js"
      via: "IPC invoke settings:setTranslationSettings"
      pattern: "ipcRenderer\\.invoke.*setTranslationSettings"
---

<objective>
Add translation preferences (toggle and language selector) to Settings view with persistence.

Purpose: Users can enable/disable translation and select target language in Settings panel.

Output: Modified SettingsView.js with translation toggle and language dropdown, IPC handlers in settingsService.js, preload bridge methods.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-settings-ui/03-CONTEXT.md
@.planning/phases/03-settings-ui/03-RESEARCH.md
@.planning/phases/02-translation-service/02-01-SUMMARY.md
@src/features/settings/settingsService.js
@src/preload.js
@src/ui/settings/SettingsView.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add translation settings IPC handlers to settingsService.js</name>
  <files>src/features/settings/settingsService.js</files>
  <action>
1. Add translation defaults to electron-store initialization:
   - translationEnabled: false (default off per CONTEXT.md)
   - translationLanguage: 'en' (default English per CONTEXT.md)

2. Add two IPC handlers:
   - ipcMain.handle('settings:getTranslationSettings', ...) - returns {enabled, language}
   - ipcMain.handle('settings:setTranslationSettings', ...) - accepts {enabled, language}, saves and notifies windows

3. Follow existing pattern in settingsService.js:
   - Use getSettings() to retrieve current settings
   - Use windowNotificationManager.notifyRelevantWindows() after save
   - Return structured response

Reference RESEARCH.md lines 286-316 for exact code patterns.
  </action>
  <verify>
Check that IPC handlers can be invoked:
- Read settingsService.js to verify handlers registered with ipcMain.handle
- Verify defaults added to Store initialization
- Verify notification after save
  </verify>
  <done>
settingsService.js has getTranslationSettings and setTranslationSettings handlers that persist to electron-store and notify windows of changes
</done>
</task>

<task type="auto">
  <name>Task 2: Expose translation settings via preload.js</name>
  <files>src/preload.js</files>
  <action>
Add two methods to settingsView context bridge:

1. getTranslationSettings: () => ipcRenderer.invoke('settings:getTranslationSettings')
2. setTranslationSettings: (settings) => ipcRenderer.invoke('settings:setTranslationSettings', settings)

Follow existing pattern for settings methods (lines 237-243 in preload.js).
  </action>
  <verify>
Read preload.js to verify:
- settingsView.getTranslationSettings method exists
- settingsView.setTranslationSettings method exists
- Both use ipcRenderer.invoke pattern
  </verify>
  <done>
preload.js exposes window.settingsView.getTranslationSettings and window.settingsView.setTranslationSettings to renderer
</done>
</task>

<task type="auto">
  <name>Task 3: Add translation UI to SettingsView.js</name>
  <files>src/ui/settings/SettingsView.js</files>
  <action>
Add translation settings section to SettingsView.js:

1. Add new component properties (after line 507):
   - translationEnabled: { type: Boolean, state: true }
   - translationLanguage: { type: String, state: true }
   - isLanguageDropdownOpen: { type: Boolean, state: true }
   - languageSearchQuery: { type: String, state: true }

2. Initialize in constructor:
   - this.translationEnabled = false
   - this.translationLanguage = 'en'
   - this.isLanguageDropdownOpen = false
   - this.languageSearchQuery = ''

3. Add LANGUAGES constant (26 common languages per RESEARCH.md lines 257-283):
   ```javascript
   const LANGUAGES = [
       { code: 'en', name: 'English' },
       { code: 'es', name: 'Spanish' },
       // ... all 26 languages
   ];
   ```

4. Add computed property for filtered languages:
   ```javascript
   get filteredLanguages() {
       const query = this.languageSearchQuery.toLowerCase();
       return LANGUAGES.filter(lang =>
           lang.name.toLowerCase().includes(query) ||
           lang.code.toLowerCase().includes(query)
       );
   }
   ```

5. Add CSS for toggle-switch and language-dropdown (in static styles):
   - .toggle-switch: iOS-style switch per RESEARCH.md lines 148-176
   - .dropdown-container, .dropdown-list, .dropdown-item for searchable dropdown

6. Add loadTranslationSettings() call in connectedCallback:
   - Call window.settingsView.getTranslationSettings()
   - Set translationEnabled and translationLanguage from response

7. Add translation section in render() method:
   - Place after "Automatic Updates" toggle (per RESEARCH.md recommendation)
   - iOS-style toggle switch for translationEnabled
   - Searchable dropdown for language selection (only visible when translationEnabled is true, or always visible but disabled when off)

8. Add handlers:
   - toggleTranslation(): calls setTranslationSettings with toggled enabled value
   - selectLanguage(lang): calls setTranslationSettings with selected language
   - openLanguageDropdown(), closeLanguageDropdown()

Follow existing CSS and component patterns in SettingsView.js.
  </action>
  <verify>
Read SettingsView.js to verify:
- Properties added: translationEnabled, translationLanguage, isLanguageDropdownOpen, languageSearchQuery
- LANGUAGES constant defined
- filteredLanguages computed property exists
- CSS classes for toggle-switch and dropdown added
- Load function called in connectedCallback
- Render section includes toggle and dropdown
- Event handlers for toggle and dropdown implemented
  </verify>
  <done>
SettingsView.js displays translation toggle (iOS-style switch) and searchable language dropdown; settings load from electron-store on mount and save on change
</done>
</task>

</tasks>

<verification>
Phase-level checks after all tasks:
- [ ] Translation toggle renders in Settings panel (iOS-style switch)
- [ ] Language dropdown shows 20+ languages with search filter
- [ ] Toggle switches between enabled/disabled states
- [ ] Selecting language updates the setting
- [ ] Settings persist after app restart (electron-store)
- [ ] Other settings components still function correctly
</verification>

<success_criteria>
- SETT-01: Toggle shows in Settings view, toggling updates state and persists
- SETT-02: Dropdown shows 26 languages with search filtering, selection persists
- SETT-03: After app restart, translation settings retain previous values
</success_criteria>

<output>
After completion, create `.planning/phases/03-settings-ui/03-01-SUMMARY.md`
</output>
