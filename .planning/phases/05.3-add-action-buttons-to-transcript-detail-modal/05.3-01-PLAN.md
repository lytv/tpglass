---
phase: 05.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/listen/stt/SttView.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can see Edit, Summarize, Custom Prompt buttons in modal header"
    - "User can edit original transcript text inline with Save/Cancel controls"
    - "User can click Summarize to open new modal with summary content"
    - "User can click Custom Prompt to open new modal"
  artifacts:
    - path: "src/ui/listen/stt/SttView.js"
      provides: "Modal with action buttons, inline edit, summarize/custom-prompt modals"
      min_lines: 200
  key_links:
    - from: "SttView.js modal-header"
      to: "_handleEdit, _handleSummarize, _handleCustomPrompt"
      via: "click handlers"
      pattern: "@click.*handle"
---

<objective>
Add action buttons (Edit, Summarize, Custom Prompt) to the translation detail modal in SttView.js. This includes inline editing for transcripts, and separate modals for summarize and custom prompt functionality.

Purpose: Enhance the transcript modal with actionable AI features
Output: Modified SttView.js with 3 action buttons and associated functionality
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ui/listen/stt/SttView.js

From 05.3-CONTEXT.md (user decisions - LOCKED):
- Button placement: modal header, next to close (X) button
- Order: [Edit] [Summarize] [Custom Prompt] [X]
- Button styling: emoji icons (✏️ 📝 ⚡), 14px size, subtle hover states
- Edit behavior: inline editable text for original text only, Save/Cancel controls
- Summarize: separate modal, TLDR + bullet points + action items, uses both original and translated text
- Custom Prompt: separate modal, placeholder UI (prompts from Settings - future phase)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add action buttons to modal header</name>
  <files>src/ui/listen/stt/SttView.js</files>
  <action>
1. Add action button CSS styles in the static styles block:
   - .action-buttons container with flex layout
   - .action-btn class: transparent bg, 14px font-size, emoji icons, hover states

2. Modify modal-header in render() to include action buttons:
   - Add div.action-buttons with 3 buttons (Edit, Summarize, Custom Prompt)
   - Order: Edit (✏️), Summarize (📝), Custom Prompt (⚡), then X close button

3. Add state properties:
   - _isEditing: boolean for edit mode
   - _editText: string for edited text
   - _isSummarizing: boolean for loading state
   - _summaryContent: object for summarize results
   - _showSummaryModal: boolean
   - _showCustomPromptModal: boolean

4. Add handler methods:
   - _handleEdit(): toggle _isEditing
   - _handleSaveEdit(): save changes, close edit mode
   - _handleCancelEdit(): revert _editText, close edit mode
   - _handleSummarize(): call summarize API, open summary modal
   - _handleCustomPrompt(): open custom prompt modal (placeholder)

5. Reset edit state when modal closes in _handleTranslationClick
  </action>
  <verify>
    <automated>MISSING — Manual verification required: Open app, click translated message, verify 3 buttons appear in header</automated>
  </verify>
  <done>Modal header shows 3 action buttons: Edit (✏️), Summarize (📝), Custom Prompt (⚡)</done>
</task>

<task type="auto">
  <name>Task 2: Implement inline edit mode</name>
  <files>src/ui/listen/stt/SttView.js</files>
  <action>
1. Add edit-related CSS:
   - .edit-textarea: full-width, min-height 100px, dark theme styling
   - .edit-controls: flex container with Save/Cancel buttons

2. Modify modal-content rendering:
   - If _isEditing: show textarea with _editText, show Save/Cancel buttons
   - If not editing: show original text (read-only)

3. Implement _handleSaveEdit:
   - Update the message text in sttMessages array
   - Reset _isEditing to false
   - Dispatch 'stt-messages-updated' event

4. Implement _handleCancelEdit:
   - Reset _editText to original text
   - Reset _isEditing to false
  </action>
  <verify>
  <automated>MISSING — Manual verification required: Click Edit button, modify text, click Save, verify change persists</automated>
  </verify>
  <done>Edit button opens inline textarea, Save persists changes, Cancel reverts</done>
</task>

<task type="auto">
  <name>Task 3: Implement Summarize modal</name>
  <files>src/ui/listen/stt/SttView.js</files>
  <action>
1. Add summarize modal CSS:
   - .summary-modal-overlay, .summary-modal similar to translation-modal
   - .summary-section for TLDR, bullets, action items

2. Add _handleSummarize method:
   - Get original text and translated text for the expanded translation
   - Call window.api.summarize.summarize() via IPC
   - Set _summaryContent with response, set _showSummaryModal = true

3. Add summarize modal rendering:
   - Show loading state while _isSummarizing
   - Display TLDR section, bullet points, action items
   - Add Close button

4. Add cleanup: reset _summaryContent, _showSummaryModal when main modal closes
  </action>
  <verify>
  <automated>MISSING — Manual verification required: Click Summarize, verify new modal shows loading then summary content</automated>
  </verify>
  <done>Summarize button opens separate modal with TLDR, bullet points, action items</done>
</task>

<task type="auto">
  <name>Task 4: Implement Custom Prompt modal (placeholder)</name>
  <files>src/ui/listen/stt/SttView.js</files>
  <action>
1. Add custom prompt modal CSS (similar to summarize modal)

2. Add _handleCustomPrompt method:
   - Set _showCustomPromptModal = true (placeholder for now)

3. Add custom prompt modal rendering:
   - Show placeholder message: "Configure prompts in Settings" (future feature)
   - Show Close button

4. Per CONTEXT: Custom prompts managed in Settings view (future phase)
   - This is intentionally a placeholder until Settings phase implements prompt storage
  </action>
  <verify>
  <automated>MISSING — Manual verification required: Click Custom Prompt, verify placeholder modal appears</automated>
  </verify>
  <done>Custom Prompt button opens modal with placeholder message</done>
</task>

</tasks>

<verification>
Manual testing checklist:
1. Open app, start listening, wait for translation
2. Click on translated text to open modal
3. Verify 3 buttons appear in header: Edit, Summarize, Custom Prompt
4. Click Edit, verify textarea appears, modify text, Save/Cancel
5. Click Summarize, verify new modal opens with summary
6. Click Custom Prompt, verify placeholder modal opens
</verification>

<success_criteria>
- [ ] 3 action buttons visible in modal header
- [ ] Edit mode shows inline textarea with Save/Cancel
- [ ] Summarize opens new modal with content
- [ ] Custom Prompt opens placeholder modal
- [ ] All buttons have correct emoji icons (✏️ 📝 ⚡)
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-add-action-buttons-to-transcript-detail-modal/05.3-01-SUMMARY.md`
</output>
