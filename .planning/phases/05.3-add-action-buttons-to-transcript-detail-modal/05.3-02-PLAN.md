---
phase: 05.3
plan: 02
type: execute
wave: 2
depends_on:
  - 05.3-01
files_modified:
  - src/features/common/services/summarizeService.js
  - src/bridge/featureBridge.js
  - src/preload.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Summarize API endpoint exists and returns structured summary"
    - "IPC channel summarize:summarize is callable from renderer"
  artifacts:
    - path: "src/features/common/services/summarizeService.js"
      provides: "Backend summarize functionality using LLM"
      min_lines: 50
    - path: "src/bridge/featureBridge.js"
      provides: "IPC handler for summarize:summarize"
      exports: ["ipcMain.handle"]
    - path: "src/preload.js"
      provides: "Preload bridge for summarize API"
  key_links:
    - from: "SttView.js"
      to: "summarize:summarize"
      via: "window.api.summarize.summarize()"
      pattern:.api\\. "window\\summarize"
---

<objective>
Create backend summarize service and IPC handlers to support the Summarize button in the transcript modal.

Purpose: Provide API endpoint for generating summaries from transcript text
Output: summarizeService.js, featureBridge.js handler, preload.js bridge
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/common/services/translationService.js
@src/bridge/featureBridge.js
@src/preload.js

Following the existing pattern from TranslationService.js for LLM calls.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create summarizeService.js</name>
  <files>src/features/common/services/summarizeService.js</files>
  <action>
1. Create new file: src/features/common/services/summarizeService.js

2. Follow TranslationService pattern:
   - Import createLLM from ../ai/factory
   - Import modelStateService for API key

3. Implement summarize function:
   - Input: { originalText, translatedText }
   - Build prompt requesting TLDR, bullet points, action items
   - Use createLLM with modelStateService for API key
   - Parse LLM response into structured format:
     - tldr: string
     - bulletPoints: string[]
     - actionItems: string[]

4. Error handling: return error object on failure

5. Example prompt format:
   "Analyze the following transcript and provide:
   1. TLDR (1-2 sentences)
   2. Key bullet points (3-5)
   3. Action items if any

   Original: {originalText}
   Translation: {translatedText}"
</action>
  <verify>
  <automated>MISSING — Manual verification required: After implementation, test summarize button in modal</automated>
  </verify>
  <done>summarizeService.js exports summarize() function using LLM</done>
</task>

<task type="auto">
  <name>Task 2: Add IPC handler in featureBridge.js</name>
  <files>src/bridge/featureBridge.js</files>
  <action>
1. Add require for summarizeService at top:
   const summarizeService = require('../features/common/services/summarizeService');

2. Add new IPC handler after translation handlers:
   ipcMain.handle('summarize:summarize', async (event, { originalText, translatedText }) => {
       return await summarizeService.summarize({ originalText, translatedText });
   });
</action>
  <verify>
  <automated>MISSING — Manual verification required: IPC handler registered</automated>
  </done>
  <done>featureBridge.js exposes summarize:summarize IPC channel</done>
</task>

<task type="auto">
  <name>Task 3: Add preload bridge for summarize API</name>
  <files>src/preload.js</files>
  <action>
1. Find the preload.js file and add summarize bridge:

2. Add to the api object (similar to translation bridge):
   summarize: {
       summarize: (originalText, translatedText) =>
           ipcRenderer.invoke('summarize:summarize', { originalText, translatedText }),
   }

3. The preload.js likely already has window.api structure, add summarize sibling to translation
</action>
  <verify>
  <automated>MISSING — Manual verification required: window.api.summarize.summarize() is callable</automated>
  </done>
  <done>preload.js exposes window.api.summarize API</done>
</task>

</tasks>

<verification>
Manual testing checklist:
1. After plan 01 and plan 02, click Summarize in modal
2. Verify loading state shows
3. Verify summary modal displays with TLDR, bullet points, action items
4. Verify error handling if API fails
</verification>

<success_criteria>
- [ ] summarizeService.js exists and follows TranslationService pattern
- [ ] IPC handler registered in featureBridge.js
- [ ] Preload bridge enables window.api.summarize.summarize() call
- [ ] Summarize modal receives structured summary data
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-add-action-buttons-to-transcript-detail-modal/05.3-02-SUMMARY.md`
</output>
