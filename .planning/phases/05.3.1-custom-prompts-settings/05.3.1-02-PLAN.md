---
phase: 05.3.1
plan: 02
type: execute
wave: 2
depends_on: [05.3.1-01]
files_modified:
  - src/ui/settings/SettingsView.js
  - src/ui/listen/stt/SttView.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can view custom prompts in Settings section"
    - "Users can create new custom prompts in Settings"
    - "Users can edit existing custom prompts in Settings"
    - "Users can delete custom prompts in Settings"
    - "Users cannot edit/delete default prompts (read-only)"
    - "Custom Prompt modal shows dropdown with available prompts"
    - "Running prompt displays result in modal"
    - "Error states display appropriately"
  artifacts:
    - path: "src/ui/settings/SettingsView.js"
      provides: "Custom Prompts CRUD UI"
      contains: "Custom Prompts"
    - path: "src/ui/listen/stt/SttView.js"
      provides: "Custom Prompt modal with dropdown and execution"
      contains: "customPromptPresets"
  key_links:
    - from: "src/ui/settings/SettingsView.js"
      to: "src/features/settings/settingsService.js"
      via: "window.api.settingsView.getPresets()"
      pattern: "getPresets"
    - from: "src/ui/listen/stt/SttView.js"
      to: "src/features/common/services/summarizeService.js"
      via: "window.api.customPrompt.run()"
      pattern: "customPrompt\\.run"
---

<objective>
Implement UI for custom prompts: Add Custom Prompts section in SettingsView for CRUD operations, and update SttView modal with dropdown + execution.

Purpose: Allow users to create, edit, delete custom prompts in Settings and execute them from the Custom Prompt modal in transcript view.

Output: Complete custom prompts UI feature
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3.1-custom-prompts-settings/05.3.1-CONTEXT.md
@.planning/phases/05.3.1-custom-prompts-settings/05.3.1-RESEARCH.md
@.planning/phases/05.3-add-action-buttons-to-transcript-detail-modal/05.3-01-SUMMARY.md

From SettingsView.js (existing preset section pattern):
- Uses preset-section, preset-header, preset-list, preset-item CSS classes
- Has toggle expand/collapse functionality
- Shows presets filtered by is_default === 0 (custom only)

From SttView.js (existing Custom Prompt modal):
- Modal placeholder exists with "Configure prompts in Settings" message
- Needs dropdown for prompt selection, Run button, results display

Available APIs from Plan 01:
- window.api.customPrompt.run(prompt, transcriptText) - execute prompt
- window.api.settingsView.getPresets() - get all presets (including defaults)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Custom Prompts section in SettingsView.js</name>
  <files>src/ui/settings/SettingsView.js</files>
  <action>
Add a new "Custom Prompts" section below the translation section in SettingsView.js:

1. Add state properties:
   - showCustomPrompts (boolean, default false)
   - customPrompts (array, filtered from presets)

2. Add methods:
   - toggleCustomPrompts() - toggle showCustomPrompts
   - handleCreatePrompt() - open inline form to create new prompt
   - handleEditPrompt(preset) - open inline form to edit prompt
   - handleDeletePrompt(preset) - delete prompt with confirmation

3. Add UI section similar to existing preset section:
   - Section header with title "Custom Prompts" and toggle arrow
   - List showing custom prompts (is_default === 0) with Edit/Delete buttons
   - "Add Custom Prompt" button
   - Hidden by default (showCustomPrompts = false)
   - Read-only indicator for default prompts (is_default = 1)

4. Use window.api.settingsView.getPresets() to fetch presets
   - Filter: presets.filter(p => p.is_default === 0) for custom prompts
   - Default prompts (is_default === 1) shown as read-only

The section should follow the same styling as existing preset sections.
  </action>
  <verify>
File exists: src/ui/settings/SettingsView.js
Verify Custom Prompts section: grep -n "Custom Prompts" src/ui/settings/SettingsView.js
  </verify>
  <done>SettingsView has Custom Prompts section with list display and inline Create/Edit/Delete actions</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Custom Prompt modal in SttView.js</name>
<files>src/ui/listen/stt/SttView.js</files>
<action>
Update the Custom Prompt modal in SttView.js to be functional:

1. Add state properties:
   - customPromptPresets (array) - fetched presets filtered by is_default
   - selectedPromptId (string) - currently selected prompt ID
   - customPromptResult (string) - execution result
   - customPromptLoading (boolean) - loading state

2. Add methods:
   - loadCustomPromptPresets() - fetch presets on modal open
   - handleRunCustomPrompt() - execute selected prompt via window.api.customPrompt.run()
   - getPromptById(id) - helper to get prompt object from ID
   - _closeCustomPromptModal() - already exists, ensure it resets state

3. Update modal content:
   - If no presets: show message "No prompts configured. Create prompts in Settings."
   - If presets exist: show dropdown to select prompt
   - Show prompt preview after selection
   - Show "Run Prompt" button (only when prompt selected)
   - Display result in modal when available
   - Show loading state during execution
   - Display errors inline

4. Use transcript text (originalText) from the message as input to custom prompt

The modal should reuse the existing summary modal styling pattern.
</action>
  <verify>
File exists: src/ui/listen/stt/SttView.js
Verify Custom Prompt modal is functional: grep -n "customPromptPresets\|handleRunCustomPrompt" src/ui/listen/stt/SttView.js
</verify>
  <done>SttView Custom Prompt modal has dropdown, Run button, and displays results</done>
</task>

</tasks>

<verification>
- [ ] SettingsView.js has Custom Prompts section
- [ ] SttView.js Custom Prompt modal is functional with dropdown
- [ ] Both sections work with existing preset infrastructure
</verification>

<success_criteria>
Custom prompts feature is complete:
- Users can create, edit, delete custom prompts in Settings
- Users can select and run prompts from transcript modal
- Default "Summarize" prompt is visible but read-only
- Execution results display in modal
</success_criteria>

<output>
After completion, create `.planning/phases/05.3.1-custom-prompts-settings/05.3.1-02-SUMMARY.md`
</output>
